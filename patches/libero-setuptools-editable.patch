diff --git a/setup.py b/setup.py
index c479cec..8d62477 100644
--- a/setup.py
+++ b/setup.py
@@ -15,7 +15,9 @@ long_description = "".join(lines)
 
 setup(
     name="libero",
-    packages=[package for package in find_packages() if package.startswith("libero")],
+    packages=["libero"]
+    + [f"libero.{package}" for package in find_packages(where="libero")],
+    package_dir={"libero": "libero"},
     install_requires=[],
     eager_resources=["*"],
     include_package_data=True,
